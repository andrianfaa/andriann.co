/* eslint-disable import/no-anonymous-default-export */
export default {
  // baseUrl: (path: string = '') => `http://localhost:3000${path}`, // Development
  baseUrl: (path: string = '') => `https://andriann.co${path}`, // Production
};
